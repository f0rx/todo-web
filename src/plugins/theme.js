import colors from "vuetify/es5/util/colors";

export default {
  primary: {
    base: colors.grey.lighten1, // modifies `primary`
    darken1: colors.grey.darken // modifies `primary darken-1`
  }
};
